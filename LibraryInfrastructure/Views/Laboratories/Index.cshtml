@model IEnumerable<LibraryDomain.Models.Laboratory>

@{
    ViewData["Title"] = "Лабораторії";
}

<h1>Лабораторії</h1>

@if (TempData["DeleteError"] != null)
{
    <div class="alert alert-danger">
        @TempData["DeleteError"]
    </div>
}

<!-- Кнопка "Створити нову" показується лише для авторизованих -->
@if (User.Identity.IsAuthenticated)
{
    <p>
        <a asp-action="Create">Створити нову</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LabNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.LabNumber)
                </td>
                <td>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id">Змінити</a>
                        <span> | </span>
                    }

                    <a asp-action="Details" asp-route-id="@item.Id">Деталі</a>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <span> | </span>
                        <a asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
